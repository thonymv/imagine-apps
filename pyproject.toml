[project]
name = "prueba-tecnica-imagine-apps"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.25,<0.12.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = ["apps/api"]

[tool.fastapi]
entrypoint = "app.main:app"
